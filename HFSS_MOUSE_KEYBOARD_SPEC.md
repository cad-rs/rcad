# HFSS 风格几何创建交互规范（Mouse / Keyboard Spec）

> 目的：总结 **Ansys HFSS / 主流 3D 电磁 CAD** 中常见的鼠标与键盘交互方式，形成 `RCAD` 下一阶段几何创建与鼠标建模的参考规范。  
> 说明：本文以**交互范式**为主，强调一致性、可实现性与 CAD 友好性；具体热键允许在实现阶段做可配置化。

---

## 1. 设计目标

### 1.1 目标
- 支持类似 HFSS 的**点击式几何体创建**流程。
- 将 **视图导航** 与 **几何创建** 清晰分离，避免误操作。
- 为 `creator-egui` / `creator-iced` 后续的输入系统设计提供统一规范。

### 1.2 适用范围
- 2D 草图创建：`Line`、`Rectangle`、`Circle`、`Arc`、`Ring`、`Ellipse`、`Polyline`、`B-Spline Curve`。
- 3D 实体创建：`Box`、`Cylinder`、`Sphere`、`Cone`、`Torus`、`Extrude`、`Revolve`。
- 选择与编辑：单选、多选、框选、取消、删除。
- 视口控制：旋转、平移、缩放、聚焦。

---

## 2. 核心交互原则

1. **左键优先用于建模确认**  
   - 在创建命令激活时，左键承担“落点 / 确认 / 完成当前步骤”的职责。

2. **导航动作必须与建模动作解耦**  
   - 视图旋转、平移、缩放不应抢占创建命令的左键流程。
   - 推荐使用 `中键 / 滚轮 / Alt+左键` 承担视图导航。

3. **每一步都必须有预览反馈**  
   - 鼠标移动时实时显示橡皮筋线、预览框、半透明实体、尺寸提示。

4. **Esc 永远可取消**  
   - 用户应随时可以用 `Esc` 取消当前命令或退出当前步骤。

5. **Enter 用于完成，Backspace 用于回退**  
   - 尤其适用于多段线、路径、轮廓类命令。

6. **修饰键仅做增强，不改变主流程**  
   - `Shift`、`Ctrl`、`Alt` 用于约束、增选、导航等增强能力，不应破坏基本点击建模闭环。

---

## 3. 鼠标行为规范总表

| 输入 | 空闲/浏览模式 | 几何创建模式 | 备注 |
|---|---|---|---|
| `左键单击` | 选择对象 / 面 / 边 / 顶点 | 设置起点、中心点、终点或确认当前步骤 | 主确认键 |
| `左键拖动` | 框选（若无命令激活） | 拖拽定义矩形范围、方向或尺寸 | 应显示动态预览 |
| `左键双击` | 进入编辑 / 聚焦对象（可选） | 结束 `Polyline` / `Profile` 命令 | 推荐作为“快速完成” |
| `右键单击` | 打开上下文菜单 | 打开“完成/取消/输入参数”菜单，或结束子步骤 | 不建议作为主确认键 |
| `中键拖动` | 平移视图 | 平移视图 | 建模时仍可用 |
| `滚轮` | 缩放视图 | 缩放视图 | 以当前光标为缩放中心更佳 |
| `Alt + 左键拖动` | 旋转/Orbit 视图 | 旋转/Orbit 视图 | 推荐作为统一 3D 旋转方式 |
| `Shift + 中键拖动` | 可选：增强平移或轴向平移 | 同左 | 可保留扩展 |

> **推荐落地策略**：后续 RCAD 从当前“左键拖动旋转”逐步过渡为：  
> - `左键`：创建 / 选择  
> - `中键`：平移  
> - `滚轮`：缩放  
> - `Alt + 左键`：旋转  
>
> 这样更接近专业 CAD / CAE 软件的操作习惯，也更适合“点击创建几何体”。

---

## 4. 键盘行为规范总表

| 按键 | 作用 | 优先级 | 说明 |
|---|---|---:|---|
| `Esc` | 取消当前命令 / 退出当前步骤 / 清空预选 | 最高 | 必须全局可用 |
| `Enter` | 完成当前命令 / 确认参数 | 高 | 对多步命令尤其重要 |
| `Backspace` | 撤销上一个点 | 高 | 用于 `Polyline`、路径类命令 |
| `Delete` | 删除当前选中对象 | 中 | 删除前可要求二次确认 |
| `Ctrl` | 多选 / 加选 / 减选 | 中 | 推荐 `Ctrl+Click` 实现增删选 |
| `Shift` | 角度约束 / 轴向约束 / 精确模式 | 中 | 例如锁定水平/垂直/45° |
| `Alt` | 临时进入视图旋转 | 中 | 与鼠标组合时优先于创建 |
| `Tab` | 在数值输入框/参数项之间切换 | 中 | 配合尺寸驱动输入 |
| `X` / `Y` / `Z` | 锁定当前绘制轴向（推荐） | 中 | 适合 3D 草图/拉伸方向 |
| `Ctrl + Z` | 撤销 | 高 | 必备 |
| `Ctrl + Y` / `Ctrl + Shift + Z` | 重做 | 高 | 必备 |
| `F` | 聚焦当前选择（推荐） | 低 | 视图辅助 |

---

## 5. 典型几何创建流程

### 5.1 Box（长方体）
1. 用户选择 `Box` 命令。
2. 鼠标悬停时显示当前工作平面高亮。
3. **第一次左击**：确定基准角点。
4. 移动鼠标，实时预览底面矩形。
5. **第二次左击**：确定底面另一角点。
6. 移动鼠标或输入数值，预览拉伸高度。
7. **第三次左击** 或 `Enter`：确认高度，完成创建。
8. `Esc`：在任何一步取消。

### 5.2 Rectangle（矩形）
1. 激活 `Rectangle`。
2. **第一次左击**：确定第一个角点。
3. 移动鼠标显示预览矩形。
4. **第二次左击**：确认对角点，结束。
5. `Shift`：锁定正方形或轴向约束（推荐）。

### 5.3 Circle / Cylinder（圆 / 圆柱）
1. 激活命令。
2. **第一次左击**：确定圆心。
3. 移动鼠标显示半径预览。
4. **第二次左击**：确认半径。
5. 若为 `Cylinder`，继续移动鼠标定义高度。
6. **第三次左击** 或 `Enter`：完成实体。

### 5.4 Sphere（球体）
1. 激活 `Sphere` 命令。
2. **第一次左击**：确定球心。
3. 移动鼠标显示球半径的动态预览。
4. **第二次左击**：确认半径并完成球体创建。
5. 可选：支持直接输入半径数值后按 `Enter` 完成。
6. `Esc`：取消当前命令。

### 5.5 Cone（圆锥）
1. 激活 `Cone` 命令。
2. **第一次左击**：确定底面圆心。
3. 移动鼠标显示底面半径预览。
4. **第二次左击**：确认底面半径。
5. 移动鼠标或输入数值，预览圆锥高度与轴向。
6. **第三次左击** 或 `Enter`：确认高度并完成创建。
7. 可选扩展：支持继续输入顶面半径，兼容截锥（Frustum）模式。

### 5.6 Torus（圆环体）
1. 激活 `Torus` 命令。
2. **第一次左击**：确定圆环中心。
3. 移动鼠标显示主半径（Major Radius）预览。
4. **第二次左击**：确认主半径。
5. 继续移动鼠标显示管半径（Minor Radius）预览。
6. **第三次左击** 或 `Enter`：确认管半径并完成创建。
7. `Shift` 可用于锁定环面法向或工作平面方向（推荐）。

### 5.7 Arc（圆弧）
1. 激活 `Arc` 命令。
2. **第一次左击**：确定起点。
3. **第二次左击**：确定终点，系统显示候选圆弧预览。
4. 移动鼠标或指定第三点，确定圆弧鼓起方向与曲率。
5. **第三次左击**：确认圆弧形状并完成创建。
6. 可选：支持 `Center-Start-End`、`3-Point Arc` 两种模式切换。

### 5.8 Ring（二维环）
1. 激活 `Ring` 命令。
2. **第一次左击**：确定环中心。
3. 移动鼠标显示外半径预览。
4. **第二次左击**：确认外半径。
5. 继续移动鼠标显示内半径预览。
6. **第三次左击** 或 `Enter`：确认内半径并完成闭合环区域创建。
7. 系统应校验 `inner_radius < outer_radius`。

### 5.9 Ellipse（椭圆）
1. 激活 `Ellipse` 命令。
2. **第一次左击**：确定椭圆中心。
3. 移动鼠标定义长轴方向与半长轴长度。
4. **第二次左击**：确认长轴。
5. 移动鼠标定义短轴半径。
6. **第三次左击** 或 `Enter`：确认短轴并完成椭圆创建。
7. `Shift` 可用于锁定为正圆或主轴对齐。

### 5.10 B-Spline Curve（B样条曲线）
1. 激活 `B-Spline` 命令。
2. 每次 **左击** 添加一个控制点或拟合点。
3. 鼠标移动时显示曲线实时预览。
4. `Backspace`：删除上一个控制点。
5. `Double Click` 或 `Enter`：结束曲线输入并生成样条。
6. 可选：支持切换 `Interpolation`（插值）与 `Control Polygon`（控制多边形）模式。
7. 可选：支持设置阶数 `degree`、是否闭合 `closed`、端点切线约束。

### 5.11 Polyline（多段线）
1. 激活 `Polyline`。
2. 每次 **左击** 新增一个顶点。
3. 移动鼠标显示当前橡皮筋线段。
4. `Backspace` 删除上一个点。
5. `Double Click` 或 `Enter` 结束。
6. `Esc` 取消整个命令。

---

## 6. 选择与编辑规范

### 6.1 选择规则
- `左击空白处`：取消当前选择。
- `左击对象`：单选该对象。
- `Ctrl + 左击`：增选/减选对象。
- `左键拖拽框选`：在无创建命令激活时触发框选。

### 6.2 预选（Pre-highlight）
- 鼠标悬停对象时应有轻量高亮：
  - 顶点：高亮点
  - 边：亮色线
  - 面：半透明覆盖
- 预选高亮不等于已选中，颜色应不同。

### 6.3 右键菜单建议项
- `Done / Finish`
- `Cancel`
- `Undo Last Point`
- `Properties`
- `Delete`

---

## 7. 视图导航与建模优先级

为避免交互冲突，建议采用如下优先级：

1. **命令取消类**：`Esc`
2. **活动创建命令**：`左键点击 / Enter / Backspace`
3. **导航命令**：`Alt + 左键`、`中键拖动`、`滚轮`
4. **普通选择行为**：空闲状态下的左键选择

### 规则说明
- 当 `Box` / `Polyline` / `Circle` 等命令处于激活状态时：
  - `左键` 只用于建模步骤，不再触发视图旋转。
- 当按住 `Alt` 时：
  - 强制进入导航模式，允许临时 Orbit。
- 当用户未激活任何命令时：
  - `左键` 用于选择；`拖拽` 可用于框选。

---

## 8. 状态机建议（供实现使用）

```text
Idle
 ├─(select command)────────────▶ CommandArmed
 ├─(left click on object)──────▶ Selected
 └─(Alt/MMB navigation)────────▶ Navigating

CommandArmed
 ├─(first left click)──────────▶ FirstPointSet
 ├─(Esc)───────────────────────▶ Idle
 └─(right click cancel)────────▶ Idle

FirstPointSet
 ├─(mouse move)────────────────▶ Previewing
 ├─(left click)────────────────▶ SecondPointSet / Commit
 ├─(Backspace)─────────────────▶ CommandArmed
 └─(Esc)───────────────────────▶ Idle

Previewing
 ├─(left click)────────────────▶ NextStep or Commit
 ├─(Enter)─────────────────────▶ Commit
 └─(Esc)───────────────────────▶ Idle
```

---

## 9. RCAD 输入系统落地建议

### 9.1 事件抽象
后续输入层建议统一抽象为：

```rust
PointerDown { button, screen_pos, modifiers }
PointerUp   { button, screen_pos, modifiers }
PointerMove { screen_pos, delta, modifiers }
Wheel       { delta, screen_pos, modifiers }
KeyDown     { key, modifiers }
KeyUp       { key, modifiers }
```

### 9.2 建模命令接口建议
每个建模命令实现统一接口：
- `on_pointer_down()`
- `on_pointer_move()`
- `on_pointer_up()`
- `on_key_down()`
- `preview()`
- `commit()`
- `cancel()`

### 9.3 必须保留的数据
- 当前活动命令 `active_tool`
- 当前工作平面 `active_plane`
- 光标射线 `pick_ray`
- 命中对象 `hovered_entity`
- 当前预览几何 `preview_shape`
- 当前步骤索引 `step_index`
- 修饰键状态 `modifiers`

---

## 10. 与当前原型的差异说明

当前仓库中的原型交互更偏向**模型浏览器**：
- `creator-egui`：`左键拖动` 直接旋转视图。
- `creator-iced`：`左键按下 + 移动` 直接旋转视图。

为进入真正的 CAD 建模阶段，建议改为：

| 当前行为 | 目标行为 |
|---|---|
| `左键拖动 = 旋转` | `左键 = 选择/建模` |
| 无明确命令状态 | 引入 `Idle / Command / Preview / Commit` 状态机 |
| 仅支持旋转观察 | 增加平移、缩放、吸附、预览、数值输入 |

---

## 11. 建议的最小 MVP 实现顺序

1. **统一输入事件层**（鼠标、滚轮、键盘、修饰键）
2. **视图导航解耦**（`MMB` 平移、`Wheel` 缩放、`Alt+LMB` 旋转）
3. **选择系统**（单选、增选、预选高亮）
4. **第一批创建命令：Rectangle / Circle / Arc / Box / Sphere**
5. **第二批曲线与区域命令：Ellipse / Ring / Polyline / B-Spline**
6. **第三批实体命令：Cone / Torus**
7. **预览系统**（橡皮筋线、半透明体）
8. **撤销/取消/完成**（`Esc` / `Enter` / `Ctrl+Z`）
9. **吸附系统**（顶点、边中点、网格、工作平面）

---

## 12. 结论

如果下一步 RCAD 要做“**像 HFSS 一样通过鼠标点击来创建几何体**”的体验，建议遵守以下最小规范：

- **左键负责创建，导航不要占用左键主流程**。
- **所有创建命令都采用多步点击 + 实时预览 + Enter 完成 + Esc 取消**。
- **统一状态机和输入事件抽象**，避免后续 `egui` / `iced` 分叉实现。
- **先完成 `Rectangle / Circle / Arc / Box / Sphere / Polyline`** 的交互闭环，再扩展到 `Ellipse / Ring / B-Spline / Cone / Torus`，即可覆盖大部分基础建模交互骨架。

这份文档可作为下一阶段 `CAD 鼠标建模规范` 的基础版本。