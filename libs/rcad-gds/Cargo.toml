[package]
name = "rcad-gds"
version = "0.1.0"
edition = "2024"

[dependencies]
laykit = "0.0"
rcad-kernel = { path = "../rcad-kernel" }
glam = { workspace = true }
thiserror = "2.0"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
