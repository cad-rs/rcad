[package]
name = "rcad-algorithms"
version = "0.1.0"
edition = "2024"

[dependencies]
rcad-kernel = { path = "../rcad-kernel" }
glam.workspace = true
rayon = "1.10"

[dev-dependencies]
criterion = { workspace = true }
rcad-modeling = { path = "../rcad-modeling" }

[[bench]]
name = "boolean"
harness = false
