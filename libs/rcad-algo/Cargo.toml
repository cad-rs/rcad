[package]
name = "rcad-algo"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/cad-rs/rcad"

[dependencies]
rcad-kernel = { path = "../rcad-kernel" }
rcad-brep = { path = "../rcad-brep" }
rcad-modeling = { path = "../rcad-modeling" }
glam.workspace = true
indexmap.workspace = true
rayon = "1"
tracing = "0.1"

[dev-dependencies]
rcad-brep = { path = "../rcad-brep" }
