[package]
name = "rcad-sheetmetal"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/cad-rs/rcad"

[dependencies]
rcad-kernel = { path = "../rcad-kernel" }
rcad-modeling = { path = "../rcad-modeling" }
glam.workspace = true
serde.workspace = true

[dev-dependencies]
criterion = { workspace = true }
